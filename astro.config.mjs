import { defineConfig } from 'astro';

export default defineConfig({
  deployOptions: {
    // Configuration options for GitHub Pages deployment
    site: 'https://heidianthonis.github.io/hAIdi',
    // other GitHub Pages specific options
  },
});